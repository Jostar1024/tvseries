{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.author) }}
    {{ form_row(form.releaseAt) }}
    {{ form_row(form.description) }}
    {{ form_row(form.image) }}

    <button type="submit">Ajouter le</button>
{{ form_end(form) }}